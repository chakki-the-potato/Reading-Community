# 책책책 - 독서 모임 관리

매일 10분 독서 인증과 월 1권 완독을 목표로 하는 독서 모임 관리 웹앱입니다. 카카오톡 채팅 내보내기를 파싱하여 인증 기록을 자동으로 생성하고, 벌금을 계산합니다.

## 주요 기능

- **매일 독서 인증** - 사진 증거 + 1줄 이상 감상문으로 인증 (시험 기간엔 사진만으로도 허용)
- **인증 기록 조회** - 분기별 인증 현황을 테이블로 확인
- **벌금 자동 계산** - 미인증 일수에 따른 벌금 자동 산출
- **납부 현황 관리** - 멤버별 벌금 납부 여부 추적
- **카카오톡 파싱** - PC/Android/iOS 내보내기 형식 모두 지원

## 기술 스택

| 구분 | 기술 |
|------|------|
| Frontend | HTML, CSS, JavaScript (Vanilla) |
| Backend | Python 3.13 (CLI 스크립트) |
| 데이터 저장 | CSV + JSON (파일 기반) |
| 폰트 | Pretendard Variable |

## 프로젝트 구조

```
book_meeting/
├── index.html                 # 메인 웹페이지
├── check_verification.py      # 인증 확인 CLI 스크립트
├── static/
│   ├── css/style.css
│   └── js/public.js
├── records/
│   ├── index.json             # 인증 기록 메타데이터
│   ├── payments.json          # 벌금 납부 현황
│   └── {년}/{월}/             # 분기별 인증 기록 (CSV + JSON)
├── members/                   # 멤버 목록 (git 미추적)
├── logo_image/
└── .claude/
    └── skills/                # Claude Code 운영 스킬
        ├── member-setup/      # 멤버 파일 생성
        ├── quarter-end/       # 분기 종료 인증
        ├── quarter-end-exam/  # 분기 종료 인증 (시험 기간 포함)
        ├── quarter-start/     # 새 분기 초기화
        └── announcement/      # 카카오톡 공지 생성
```

## 시작하기

### 웹페이지

`index.html`을 웹 서버를 통해 열면 인증 기록을 조회할 수 있습니다.

### 인증 확인 스크립트

카카오톡 채팅 내보내기 파일을 파싱하여 인증 기록을 생성합니다.

```bash
python3 check_verification.py \
  --members members.csv \
  --start 2026-02-01 \
  --end 2026-02-15 \
  --output records/2026/02/verification_record.csv \
  --label "2월 1분기" \
  chat_export.txt
```

#### 옵션

| 옵션 | 설명 |
|------|------|
| `chat_files` | 카카오톡 채팅 내보내기 파일 (.txt 또는 .csv) |
| `--members, -m` | 멤버 목록 CSV 파일 (필수) |
| `--start, -s` | 시작일 YYYY-MM-DD (필수) |
| `--end, -e` | 종료일 YYYY-MM-DD (필수) |
| `--output, -o` | 출력 CSV 경로 (선택) |
| `--exclude, -x` | 인증 제외일 (선택) |
| `--label, -l` | 기록 라벨, 예: "2월 1분기" (선택) |
| `--exam-start` | 시험 기간 시작일 (선택, `--exam-end`와 함께 지정) |
| `--exam-end` | 시험 기간 종료일 (선택, `--exam-start`와 함께 지정) |

### 인증 조건

하루 인증이 유효하려면 같은 날(오전 5시 기준) 아래 두 가지를 모두 충족해야 합니다:

1. **사진** - "사진", "Photo", "동영상" 등의 메시지
2. **감상문** - 첫 줄에 '읽' 포함, 최소 1줄 이상

#### 시험 기간 예외

`--exam-start`/`--exam-end` 옵션으로 지정한 날짜 범위에는 **사진만 있어도 인증**으로 인정됩니다.

- 시험 기간 내: 사진 1장 = O (감상문 없어도 OK)
- 시험 기간 내라도 사진 없이 감상문만 있으면 X
- 시험 기간 밖: 기존 규칙(사진 + 감상문) 유지

## 벌금 규칙

| 미인증 일수 | 벌금 |
|-------------|------|
| 1~5일 | 일당 2,000원 (최대 10,000원) |
| 6일 이후 | 일당 1,000원 추가 |

예시: 10일 미인증 = 10,000원 + (5 × 1,000원) = **15,000원**

## 운영

Claude Code 스킬로 분기 전환 작업을 수행합니다.

| 스킬 | 용도 |
|------|------|
| `/member-setup` | 새 분기 멤버 CSV 파일 생성 |
| `/quarter-end` | 분기 종료 인증 기록 생성 + payments.json 업데이트 |
| `/quarter-end-exam` | 동일 + 시험 기간 옵션 포함 |
| `/quarter-start` | 새 분기 물음표 상태로 초기화 |
| `/announcement` | 카카오톡 공지 텍스트 생성 |

### 벌금 납부 현황 수동 업데이트

납부한 멤버가 생기면 `records/payments.json`에 이름을 추가합니다.

```json
{
  "2026/04/verification_record_20260406_20260426.csv": ["홍길동", "김철수"]
}
```

- 키: `{YYYY}/{MM}/verification_record_{시작일}_{종료일}.csv`
- 값: 납입 완료한 멤버 이름 배열

---

## 카카오톡 공지 양식

### 벌금 정산 공지

```
○월 ○분기 벌금 정산입니다. 아래 계좌로 납입 부탁드립니다!

다들 페이지에서 본인 인증 현황 확인하시고 문제 있으면 알려주세요.

79799078100 카카오뱅크, 이찬희
```

### 일반 공지

```
몇 가지 공지드립니다.

1. 소모임 모임통장이 있습니다. 최대한 투명하게 운영하고자 현황을 공개하고 있으며, 가입을 희망하시면 이 글에 체크 부탁드립니다.

2. 감상문 작성 시 첫 문장에 반드시 '읽'이라는 글자가 들어가야 벌금 정산 시 오류가 없습니다. 제가 직접 검토하는 게 아닌 코드로 처리하고 있으니 꼭 지켜주세요.

3. 인증 기준은 매우 널널합니다. 읽기만 했다면 간단한 사진과 감상문을 올려주시면 웬만해서는 모두 인정합니다.
   (시간 기준: 새벽 5시까지, 전날 기록으로 처리 / 예: 23일 4am 인증 → 22일로 인정)

4. 소모임 취지는 '생각하기 위해 읽는 책'입니다. 책 선택은 자유이나, 만화책·전공책 등은 인정이 어렵습니다.

궁금한 점 있으시면 편하게 말씀해주세요!
```

### 모집 공지

```
뇌에 기름칠 할 사람 급구! 🧠 독서 소모임 <책책책 책을 읽읍시다> 활동멤버 모집!

"오늘도 스마트폰만 보셨나요? 당신의 뇌, 이대로 괜찮은가?"

하루 딱 10분! 부담 없이 함께 책을 읽고, 서로의 생각을 나누며 지식인이 되어봅시다.
꾸준한 독서 습관은 저희가 책임질게요.

자세한 활동 규칙과 내용은 아래 페이지에서 확인하세요! 👇

🔗 소모임 페이지: https://chakki-the-potato.github.io/Reading-Community/
```

### 분기 종료 안내

```
○월 ○분기 활동이 ○월 ○일(○)로 마무리됩니다.

다음 분기(○월 ○분기)부터 활동을 중단하고 싶으신 분은 ○월 ○일까지 저에게 개인적으로 연락 부탁드립니다.
별도 연락이 없으시면 다음 분기에도 자동으로 참여하는 것으로 처리됩니다.
```
